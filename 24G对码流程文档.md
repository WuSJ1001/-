# 24G 协议对码流程文档

## 概述

24G 对码是鼠标/键盘（发射端）与 USB Dongle（接收端）之间建立配对关系的过程，采用 4 步握手机制完成设备识别和地址交换。对码成功后双方互知对方地址，后续通过重连流程恢复数据通信。

---

## 文件结构

### 核心对码文件

| 文件 | 说明 |
|------|------|
| `program/g24_protocol_stack/24g_pair.prog` | 对码状态机和握手流程全部实现（发射端 + 接收端） |
| `format/g24_protocol_stack/24g_pair.format` | 对码数据结构定义（状态常量、内存分配） |
| `program/g24_protocol_stack/24g.prog` | 底层 RF 收发、载荷组包、ACK 机制 |
| `format/g24_protocol_stack/24g.format` | 基础 RF 数据结构（地址、通道映射、缓冲区） |
| `program/g24_protocol_stack/24g_transmitter.prog` | 发射调度、ACK 等待、重传跳频 |
| `format/g24_protocol_stack/24g_transmitter.format` | 发射端数据结构（连接状态机、重试计数等） |
| `program/g24_protocol_stack/24g_receiver.prog` | 接收调度、模式切换（BIND → SEARCH → WORK） |
| `format/g24_protocol_stack/24g_receiver.format` | 接收端数据结构（工作模式、绑定状态、ACK payload） |
| `program/g24_protocol_stack/24g_reconn.prog` | 重连流程（对码成功后恢复通信） |
| `format/g24_protocol_stack/24g_reconn.format` | 重连数据结构 |

### 应用层入口文件

| 文件 | 说明 |
|------|------|
| `program/app_mouse.prog` | 鼠标端：触发 `g24_pair_start` 进入对码 |
| `program/app_dongle.prog` | Dongle 端：主循环调用 `g24_bind_mode_auto` 处理对码 |

---

## 关键数据结构

### 对码状态机 (`mem_24g_pair_sm`)

定义于 `format/g24_protocol_stack/24g_pair.format` (行 28-38)：

| 值 | 常量 | 说明 |
|----|------|------|
| 0x00 | STATE_24G_PAIRING_NULL | 初始/空闲 |
| 0x01 | STATE_24G_PAIRING_1 | 步骤 1：发送 ATTEMP |
| 0x02 | STATE_24G_PAIRING_2 | 步骤 2：发送 BIND |
| 0x03 | STATE_24G_PAIRING_3 | 步骤 3：发送 CONFIG |
| 0x04 | STATE_24G_PAIRING_4 | 步骤 4：发送 OK |
| 0x11 | STATE_24G_PAIRING_1_WAITING_ACK | 步骤 1 等待 ACK |
| 0x12 | STATE_24G_PAIRING_2_WAITING_ACK | 步骤 2 等待 ACK |
| 0x13 | STATE_24G_PAIRING_3_WAITING_ACK | 步骤 3 等待 ACK |
| 0x14 | STATE_24G_PAIRING_4_WAITING_ACK | 步骤 4 等待 ACK |
| 0xFF | STATE_24G_PAIRING_SUCCESS | 对码成功 |

### 连接状态机 (`mem_24g_conn_sm`)

定义于 `format/g24_protocol_stack/24g_transmitter.format` (行 40-45)：

| 值 | 常量 | 说明 |
|----|------|------|
| 0 | STATE_24G_STOP | 停止 |
| 0 | STATE_24G_START | 启动 |
| 1 | STATE_24G_PAIR | 对码模式 |
| 2 | STATE_24G_RECONN | 重连模式 |

### 数据类型常量

定义于 `format/g24_protocol_stack/24g_pair.format` (行 41-48)：

| 值 | 常量 | 说明 |
|----|------|------|
| 0x01 | DATATYPE_DEVICE1 | 鼠标设备 |
| 0x02 | DATATYPE_DEVICE2 | 键盘设备 |
| 0xAA | DATATYPE_BIND | 绑定请求（Step 2） |
| 0x55 | DATATYPE_CONFIG | 配置交换（Step 3） |
| 0x22 | DATATYPE_OK | 确认完成（Step 4） |
| 0xFF | DATATYPE_ATTEMP | 连接尝试（Step 1 / 重连） |

### 关键交换地址

| 地址变量 | 所属端 | 说明 | 交换方向 |
|----------|--------|------|----------|
| `mem_24g_device_addr` | 发射端 | 鼠标/键盘的地址 | Step 1 发给 Dongle |
| `mem_24g_receiver_addr` | 发射端 | 存储 Dongle 的地址 | Step 2 从 Dongle ACK 获取 |
| `mem_24g_lap` | 接收端 | Dongle 自身的 LAP 地址 | Step 2 通过 ACK 发给发射端 |
| `mem_24g_transmitter_addr` | 接收端 | 存储发射端的地址 | Step 1 从发射端获取 |
| `mem_24g_device1_addr` | 接收端 | 鼠标地址（存储） | Step 1 保存 |
| `mem_24g_device2_addr` | 接收端 | 键盘地址（存储） | Step 1 保存 |

---

## 完整对码流程

### 阶段零：触发入口

#### 发射端（鼠标）触发

`program/app_mouse.prog` — `mouse_24g_start_pair_mode` (行 4448-4461)：

1. 配置对码 LED 指示灯
2. 设置 `mem_24g_reconn_type = 0`
3. 设置对码标志 `MOUSE_24G_PAIRING_FLAG`
4. 清零重连超时
5. 停止蓝牙发现
6. 设置设备类型：默认 `TYPE_MS`（鼠标），若启用了键盘功能则改为 `TYPE_KB`
7. 调用 `g24_pair_start` 启动对码

#### 接收端（Dongle）触发

`program/app_dongle.prog`：

- `dongle_init` (行 3-14)：注册回调函数
  - `mem_cb_24g_receive_process` → `dongle_dispatch`
  - `mem_cb_24g_data_device1` → `dongle_g24_mouse`
  - `mem_cb_24g_data_device2` → `dongle_g24_kb`
  - `mem_cb_24g_ackpayload` → `dongle_ackpayload_prep`

- `dongle_default_init` (行 17-56)：硬件初始化
  - 设置对码发射功率 `TX_POWER_F5DB`
  - 配置 PHY 模式（1M 或 2M）
  - 调用 `g24_receive_init`、`g24_pair_param_init`、`g24_chmap_param_init`

- `dongle_dispatch` (行 65-70)：模式分发
  - `mem_24g_pair_mode == 0x01` → 进入 `dongle_pc_bind` PC 对码模式

- `dongle_pc_bind` (行 146-152)：PC 对码主循环
  - 调用 `dongle_work_mode_auto` 处理工作模式
  - 调用 `g24_bind_mode_auto` 处理对码
  - 调用 `dongle_usb_dispatch` 处理 USB

---

### 阶段一：发射端初始化

`program/g24_protocol_stack/24g_pair.prog`：

#### `g24_pair_start` (行 21-39)

1. 设置 `mark_24g` 标志位
2. 读取 `mem_24g_conn_sm`，设置 `STATE_24G_PAIR`，清除 `STATE_24G_RECONN`
3. 清零重连超时 (`mem_24g_reconn_timeout = 0`)
4. 加载对码超时初始值到 `mem_24g_pair_timeout`
5. 清零 `mem_24g_pair_no_ack`
6. 调用 `g24_pair_sm_reinit` 初始化状态机

#### `g24_pair_init_param` (行 33-39)

1. 清零 `mem_24g_pid`
2. 加载对码通道 `mem_24g_pair_ch` → `mem_24g_ch`
3. 加载对码发射功率 `mem_24g_pair_tx_power` → `mem_tx_power`
4. 加载对码地址 `mem_24g_pair_addr` → 更新地址和同步字 CRC8

#### `g24_pair_sm_reinit` (行 108-113)

- 默认状态机起始为 `STATE_24G_PAIRING_2`
- 若 `mem_24g_pair_switch` 非零（快速配对模式），改为 `STATE_24G_PAIRING_1`

---

### 阶段二：发射调度入口

`program/g24_protocol_stack/24g_transmitter.prog` — `g24_transmit_dispatch` (行 29-32)：

```
读取 mem_24g_conn_sm
├── STATE_24G_PAIR 置位 → 跳 g24_pair_dispatch
└── STATE_24G_RECONN 置位 → 跳 g24_reconn_dispatch
```

#### `g24_pair_dispatch` (行 43-58)

`program/g24_protocol_stack/24g_pair.prog`：

1. 设置 `mem_24g_reconn_type = DEFAULT_24G_DEVICE`
2. 对码计数 `mem_24g_pair_count++`
3. 读取 `mem_24g_pair_sm`，按状态分发：
   - `STATE_24G_PAIRING_SUCCESS` → `g24_pair_exit`（对码已完成，退出）
   - `STATE_24G_PAIRING_1` → `g24_pair_sm_1`
   - `STATE_24G_PAIRING_2` → `g24_pair_sm_2`
   - `STATE_24G_PAIRING_3` → `g24_pair_sm_3`
   - `STATE_24G_PAIRING_4` → `g24_pair_sm_4`

---

### 阶段三：四步握手

#### 对码数据包结构

每个对码数据包 7 字节（`jam 7,mem_24g_datalen`），结构固定：

| 偏移 | 大小 | 字段 |
|------|------|------|
| `txbuf+0` | 1 | 数据类型（0xFF / 0xAA / 0x55 / 0x22） |
| `txbuf+1` | 1 | 设备类型 / data_type（TYPE_MS=1 / TYPE_KB=2，取低 3 位 bits_data） |
| `txbuf+2~5` | 4 | 地址字段（根据步骤不同填入不同地址） |
| `txbuf+6` | 1 | 0x00（填充） |

每步的通用流程（`g24_pair_sm_common`，行 86-106）：

1. 组装 data_type 到 `mem_24g_txbuf+1`
2. 调用 `g24_transmit_prep` 准备发射载荷
3. 调用 `g24_transmit_receive_ack` 发送并等待 ACK
4. 调用 `g24_txbuf_clear` 清空发送缓冲区
5. 调用 `g24_ackpayload_parse` 解析 ACK payload
6. 按当前状态跳转到对应的 `_WAITING_ACK` 处理

---

#### Step 1：连接尝试（ATTEMP）

**发射端发送**

`g24_pair_sm_1`（行 60-61）填充 `DATATYPE_ATTEMP (0xFF)`，然后跳 `g24_pair_sm_prep`（行 83-85）装入设备地址。

构造过程：

```
行 61:  jam DATATYPE_ATTEMP, mem_24g_txbuf        → txbuf[0] = 0xFF
行 84:  fetch 4, mem_24g_device_addr               → 读取发射端自身地址
行 85:  store 4, mem_24g_txbuf+2                   → txbuf[2-5] = 发射端设备地址
行 87:  fetch 1, mem_24g_data_type                 → data_type (TYPE_MS=1 或 TYPE_KB=2)
行 88:  and_into bits_data, pdata                  → 提取低 3 位
行 89:  store 1, mem_24g_txbuf+1                   → txbuf[1] = 设备类型
行 90:  jam 0, mem_24g_txbuf+6                     → txbuf[6] = 0x00
```

发射端发送载荷（7 字节）：

```
[0] = 0xFF              ← DATATYPE_ATTEMP
[1] = 0x01 / 0x02       ← 设备类型 (DEVICE1=鼠标 / DEVICE2=键盘)
[2-5]                   ← 发射端自身地址 (mem_24g_device_addr, 4字节)
[6] = 0x00              ← 填充
```

发送含义：**"我是鼠标/键盘，这是我的地址"**

**发射端收到 ACK** — `g24_pair_sm_1_waiting_ack` (行 115-117)：

- 状态机推进到 `STATE_24G_PAIRING_2`
- 调用 `g24_transmit_abandon` 结束本次发射

**接收端（Dongle）处理** — `g24_bind_data_parse` → `g24_data_attemp`

`program/g24_protocol_stack/24g_reconn.prog` (行 151-171)：

```
收到 DATA[2] == DATATYPE_ATTEMP (0xFF)
从 DATA[3] 判断设备类型:

DATATYPE_DEVICE1 (0x01) → g24_data_attemp_device1 (行 156-163):
  行 159: fetch 4, mem_24g_rxdata_temp+4       → 提取发射端地址（rxdata_temp[4-7]）
  行 160: store 4, mem_24g_transmitter_addr     → 保存发射端地址
  行 162: store 4, mem_24g_device1_addr         → 保存为鼠标地址

DATATYPE_DEVICE2 (0x02) → g24_data_attemp_device2 (行 164-171):
  行 167: fetch 4, mem_24g_rxdata_temp+4       → 提取发射端地址
  行 168: store 4, mem_24g_transmitter_addr     → 保存发射端地址
  行 170: store 4, mem_24g_device2_addr         → 保存为键盘地址
```

Dongle 收到后只**保存地址**。注意此时 Dongle 不与发射端做地址校验——任何发射端发来的 ATTEMP，只要类型匹配且该设备尚未绑定（`mem_24g_deviceX_bind_disable == 0`），Dongle 都会接受并保存地址。

ACK 回复由底层 `g24_transmit_ack` 处理，不携带特殊 payload（`mem_24g_bind_payload` 此时尚未设置，ACK 中无有效数据）。

Dongle 回复含义：**"收到"**（仅 ACK 确认）

---

#### Step 2：绑定请求（BIND）

**发射端发送**

`g24_pair_sm_2`（行 64-71）：

```
行 65:  fetch 1, mem_24g_pair_switch
行 66:  branch g24_pair_sm_2_skip, blank         ← pair_switch=0 则跳过
行 67:  fetch 4, mem_24g_device_addr              ← pair_switch=1 时
行 68:  call g24_update_addr_and_synccrc8         ← 切换使用设备地址更新同步字
行 70:  jam DATATYPE_BIND, mem_24g_txbuf          ← txbuf[0] = 0xAA
```

然后经 `g24_pair_sm_prep`（行 83-85）装载地址。

发射端发送载荷：

```
[0] = 0xAA              ← DATATYPE_BIND
[1] = 0x01 / 0x02       ← 设备类型
[2-5]                   ← 设备地址
[6] = 0x00
```

发送含义：**"请绑定我"**

**发射端收到 ACK** — `g24_pair_sm_2_waiting_ack` (行 119-126)：

```
行 120: fetch 1, mem_24g_pair_switch
行 121: branch g24_pair_sm_2_waiting_ack_skip, blank  ← pair_switch=0 跳过
行 122: fetch 4, mem_24g_rxpayload+2                  ← 从 ACK payload 偏移 2 读取
行 123: store 4, mem_24g_receiver_addr                ← 保存 Dongle 地址
行 125: jam STATE_24G_PAIRING_3, mem_24g_pair_sm       ← 状态 → STATE_3
```

`pair_switch=1` 时，发射端从 ACK 中**学到了 Dongle 的地址**，存到 `mem_24g_receiver_addr`。

**接收端（Dongle）处理** — `g24_bind_data_parse` → `g24_bind_first_step`

`program/g24_protocol_stack/24g_pair.prog` (行 244-268)：

从 `DATA[3]` 判断设备类型并准备 ACK payload：

```
Device1 (鼠标) → g24_bind_first_step_device1 (行 250-256):
  行 253: jam 1, mem_24g_device1_bind_step              ← bind_step = 1
  跳 g24_bind_dvc1_payload_cfg (行 254):
    行 255: jam DATATYPE_DEVICE1, mem_24g_bind_payload   ← payload[0] = 0x01
    行 257: jam 0x80, mem_24g_bind_payload+1             ← payload[1] = 0x80
    行 258: fetch 6, mem_24g_lap                         ← 读取 Dongle 的 LAP (6字节)
    行 259: store 6, mem_24g_bind_payload+2              ← payload[2-7] = LAP 地址

Device2 (键盘) → g24_bind_first_step_device2 (行 262-268):
  行 265: jam 1, mem_24g_device2_bind_step              ← bind_step = 1
  跳 g24_bind_dvc2_payload_cfg (行 266):
    行 267: jam DATATYPE_DEVICE2, mem_24g_bind_payload   ← payload[0] = 0x02
    跳 g24_bind_payload_cfg (行 256):                    ← payload[1] = 0x80, [2-7] = LAP
```

ACK 发送路径：`g24_bind_data_process` (行 217) → `g24_transmit_ack` (`24g_receiver.prog` 行 111-126) → `g24_ackpayload_prep` (行 12-18)，后者调用 `mem_cb_24g_ackpayload` 回调（由 `dongle_init` 注册为 `dongle_ackpayload_prep`），将 `mem_24g_bind_payload` 装入 ACK 的 txbuf。

Dongle ACK payload（8 字节）：

```
[0] = 0x01 / 0x02       ← DATATYPE_DEVICE1 或 DEVICE2
[1] = 0x80              ← 标记位
[2-7]                   ← Dongle 自己的 LAP 地址 (6字节)
```

Dongle 回复含义：**"我是 Dongle，这是我的地址（LAP）"**

---

#### Step 3：配置交换（CONFIG）

**发射端发送**

`g24_pair_sm_3`（行 73-79）：

```
行 74: jam DATATYPE_CONFIG, mem_24g_txbuf               ← txbuf[0] = 0x55
行 75: fetch 1, mem_24g_pair_switch
行 76: branch g24_pair_sm_prep, blank                   ← pair_switch=0 跳 prep（只装设备地址）
行 77: fetch 4, mem_24g_receiver_addr                   ← pair_switch=1: 读取 Dongle 地址
行 78: store 4, mem_24g_txbuf+2                          ← txbuf[2-5] = Dongle 地址
行 79: branch g24_pair_sm_common
```

`pair_switch=1` 时发送载荷：

```
[0] = 0x55              ← DATATYPE_CONFIG
[1] = 0x01 / 0x02       ← 设备类型
[2-5]                   ← Dongle 的地址 (mem_24g_receiver_addr, 上一步从 ACK 获取)
[6] = 0x00
```

发射端把上一步学到的 Dongle 地址**回传给 Dongle**。

`pair_switch=0` 时发送载荷：

```
[0] = 0x55
[1] = 0x01 / 0x02
[2-5]                   ← 发射端自己的地址 (mem_24g_device_addr)
[6] = 0x00
```

发送含义：**"我确认你的地址是 XXX"**（pair_switch=1）/ **"这是我的地址"**（pair_switch=0）

**发射端收到 ACK** — `g24_pair_sm_3_waiting_ack` (行 128-137)：

```
行 129: fetch 1, mem_24g_pair_switch
行 130: branch g24_pair_sm_3_waiting_ack_skip, blank    ← pair_switch=0 跳过
行 131: fetch 4, mem_24g_rxpayload+2                    ← 从 ACK payload 读取地址
行 132: fetcht 4, mem_24g_device_addr                    ← 对比自身地址
行 133: isub temp, null
行 134: nrtn zero                                        ← 不匹配则直接返回（拒绝）
行 136: jam STATE_24G_PAIRING_4, mem_24g_pair_sm         ← 状态 → STATE_4
```

`pair_switch=1` 时，发射端验证 Dongle ACK 中回传的地址是否等于自己的 `mem_24g_device_addr`——**双向确认完成**。

**接收端（Dongle）处理** — `g24_bind_data_parse` → `g24_bind_second_step`

`program/g24_protocol_stack/24g_pair.prog` (行 271-313)：

```
行 272: fetch 1, mem_24g_pair_switch
行 273: branch g24_bind_second_step_skip, blank          ← pair_switch=0 跳过校验
行 274: fetch 4, mem_24g_rxdata_temp+4                  ← 读取收到的地址
行 275: fetcht 4, mem_24g_lap                            ← 对比自己的 LAP
行 276: isub temp, null
行 277: nrtn zero                                        ← 不匹配则直接返回（拒绝）
```

`pair_switch=1` 时，Dongle 会**校验发射端发回来的地址是否等于自己的 LAP**——安全确认，防止配错设备。

设备状态推进和 ACK payload 准备：

```
Device1 → g24_bind_dvc1_step_set2 (行 290-300):
  行 291: jam 2, mem_24g_device1_bind_step               ← bind_step 1→2
  行 292: fetch 1, mem_24g_pair_switch
  行 293: branch g24_bind_dvc1_payload_cfg, blank        ← pair_switch=0: ACK = DEVICE1 + LAP
  行 294: g24_bind_dvc1_payload_cfg_transmitter_addr:
    行 295: jam DATATYPE_DEVICE1, mem_24g_bind_payload
    行 297: jam 0x80, mem_24g_bind_payload+1
    行 298: fetch 4, mem_24g_transmitter_addr            ← 发射端地址
    行 299: store 4, mem_24g_bind_payload+2

Device2 → g24_bind_dvc2_step_set2 (行 308-313):
  行 309: jam 2, mem_24g_device2_bind_step
  同理准备 ACK payload
```

Dongle ACK payload（`pair_switch=1` 时）：

```
[0] = 0x01 / 0x02       ← DATATYPE_DEVICE1 或 DEVICE2
[1] = 0x80
[2-5]                   ← mem_24g_transmitter_addr（发射端地址）
```

Dongle ACK payload（`pair_switch=0` 时）：

```
[0] = 0x01 / 0x02
[1] = 0x80
[2-7]                   ← LAP 地址
```

Dongle 回复含义（pair_switch=1）：**"我确认你的地址是 XXX"**，将发射端地址回传做双向校验。

---

#### Step 4：确认完成（OK）

**发射端发送**

`g24_pair_sm_4`（行 81-82）：

```
行 82: jam DATATYPE_OK, mem_24g_txbuf                   ← txbuf[0] = 0x22
```

发射端发送载荷：

```
[0] = 0x22              ← DATATYPE_OK
[1] = 0x01 / 0x02       ← 设备类型
[2-5]                   ← 设备地址
[6] = 0x00
```

发送含义：**"绑定完成"**

**发射端收到 ACK** — `g24_pair_sm_4_waiting_ack` (行 139-158)：

```
行 140: fetch 1, mem_24g_pair_switch
行 141: branch g24_pair_sm_4_waiting_ack_skip, blank    ← pair_switch=0 跳过
行 142: fetch 4, mem_24g_rxpayload+2                    ← 验证 ACK 中的地址
行 143: fetcht 4, mem_24g_receiver_addr
行 144: isub temp, null
行 145: nrtn zero

行 147: jam STATE_24G_PAIRING_SUCCESS, mem_24g_pair_sm   ← 对码成功！状态机 → 0xFF
行 148: jam BT_EVT_24G_PAIRING_COMPLETE, mem_fifo_temp   ← 通知 UI 层
行 149: call ui_ipc_send_event
行 150: setarg 0
行 151: store 2, mem_24g_pair_timeout                    ← 清零对码超时
行 152: fetch 4, mem_24g_rxpayload+2                     ← 提取地址
行 153: call g24_update_addr_and_synccrc8                ← 更新通信地址和同步字

行 154: g24_pair_exit:
行 155: fetch 1, mem_24g_conn_sm
行 156: set0 STATE_24G_PAIR, pdata                       ← 退出对码模式
行 157: store 1, mem_24g_conn_sm
行 158: branch g24_transmit_abandon
```

**接收端（Dongle）处理** — `g24_bind_data_parse` → `g24_bind_third_step`

`program/g24_protocol_stack/24g_pair.prog` (行 316-361)：

```
行 317: fetch 1, mem_24g_pair_switch
行 318: branch g24_bind_third_step_skip, blank
行 319: fetch 4, mem_24g_rxdata_temp+4
行 320: fetcht 4, mem_24g_transmitter_addr               ← 最终校验地址
行 321: isub temp, null
行 322: nrtn zero

Device1 → g24_bind_dvc1_step_success (行 335-345):
  行 336: jam 1, mem_24g_device1_bind_disable            ← 标记已绑定，不再接受新绑定
  行 339: fetch 1, mem_24g_bind_device_status
  行 340: or_into 0x01, pdata                            ← bit0 = 1（鼠标已绑定）
  行 341: store 1, mem_24g_bind_device_status
  行 342: g24_bind_exit:
    行 343: setarg 0
    行 344: store 2, mem_24g_device1_bind_step            ← 清零步骤计数器

Device2 → g24_bind_dvc2_step_success (行 353-361):
  行 354: jam 1, mem_24g_device2_bind_disable            ← 标记已绑定
  行 358: or_into 0x02, pdata                            ← bit1 = 1（键盘已绑定）
  行 361: branch dongle_g24_store_dvc2_bind_flag          ← 存储键盘绑定标志到 OTP
```

Dongle ACK payload（与 Step 3 相同）：

```
[0] = 0x01 / 0x02       ← DATATYPE_DEVICE1 或 DEVICE2
[1] = 0x80
[2-7]                   ← LAP 地址（pair_switch=0）或发射端地址（pair_switch=1）
```

Dongle 回复含义：**"绑定完成"**

---

#### 四次握手信息交换总结

```
┌─────────┬───────────────────────────────────┬──────────────────────────────────┐
│  步骤   │       发射端 → Dongle              │       Dongle → 发射端 (ACK)      │
├─────────┼───────────────────────────────────┼──────────────────────────────────┤
│    1    │ 0xFF + 设备类型 + 自身地址          │ 普通 ACK（无特殊 payload）       │
│ ATTEMP  │ "我是鼠标/键盘，这是我的地址"       │ "收到"                          │
├─────────┼───────────────────────────────────┼──────────────────────────────────┤
│    2    │ 0xAA + 设备类型 + 自身地址          │ DEVICE类型 + 0x80 + Dongle的LAP  │
│  BIND   │ "请绑定我"                        │ "我是Dongle，这是我的地址"         │
├─────────┼───────────────────────────────────┼──────────────────────────────────┤
│    3    │ 0x55 + 设备类型 + Dongle地址        │ DEVICE类型 + 0x80 + 发射端地址    │
│ CONFIG  │ "我确认你的地址是XXX"              │ "我确认你的地址是XXX"              │
│         │ (pair_switch=0: 发自身地址)        │ (pair_switch=0: 发LAP)            │
├─────────┼───────────────────────────────────┼──────────────────────────────────┤
│    4    │ 0x22 + 设备类型 + 自身地址          │ 同 Step 3                        │
│   OK    │ "绑定完成"                        │ "绑定完成"                        │
└─────────┴───────────────────────────────────┴──────────────────────────────────┘
```

**核心安全机制**：Step 2 中 Dongle 通过 ACK 把自己的 LAP 地址交给发射端，Step 3 中发射端把收到的 Dongle 地址回传验证，Dongle 再把发射端地址回传验证——这是一个**双向地址确认**的过程（仅在 `mem_24g_pair_switch` 为真时执行完整校验）。`pair_switch=0` 的简化模式跳过了地址校验，Dongle 只在 Step 2/3/4 的 ACK 中重复回复自己的 LAP，不要求发射端回传确认。

---

### 阶段四：对码完成后模式切换

#### 接收端（Dongle）模式切换

`program/g24_protocol_stack/24g_receiver.prog`：

- `g24_bind_mode_auto` (行 294-307)：
  - 检测到 `mem_24g_bind_device_status` 非零（有设备绑定成功）
  - 清零 `mem_24g_mode_switch`
  - 调用 `g24_switch_work_mode` 切换到 Search 搜索模式
  - 启用无数据定时器

- 在 Search 模式下收到设备数据包后：
  - `g24_search_mode_auto` (行 341-378) 中切为 `DONGLE_WORK` 工作模式
  - 恢复对应设备的通道号（`mem_24g_device1_ch_number` / `mem_24g_device2_ch_number`）

#### 发射端后续重连

`program/g24_protocol_stack/24g_reconn.prog` — `g24_reconn_start` (行 10-32)：

- 设置 `STATE_24G_RECONN` 到连接状态机
- 根据 `mem_24g_reconn_type` 选择重连目标（快速连接 / Dongle 地址 / 3.0 地址等）
- 发送 `DATATYPE_ATTEMP` 包发起重连
- Dongle 收到后识别设备地址匹配，恢复正常数据通信

---

## 底层 RF 收发支撑

### 发射流程

`program/g24_protocol_stack/24g.prog`：

| 函数 | 行号 | 说明 |
|------|------|------|
| `g24_transmit_prep` | 282-322 | 准备发送载荷：HEC、PDU 控制字段、数据类型、payload 数据拼接、RSSI hex |
| `g24_lr_transmit_prep` | 324-343 | LR (Long Range) 模式的载荷准备 |
| `g24_transmit` | 179-274 | 发送数据包：前导码、访问地址、白化、FEC 编码、CRC、RF 调制发射 |

`program/g24_protocol_stack/24g_transmitter.prog`：

| 函数 | 行号 | 说明 |
|------|------|------|
| `g24_transmit_receive_ack` | 142-156 | 发送后立即切换到 RX 模式等待 ACK |
| `g24_transmit_process` | 113-140 | 发射主流程：发送 → 等 ACK → 解析 ACK payload → 处理重传 |
| `g24_retransmit` | 158-174 | 重传机制：ACK 超时或失败时重试，最多 N 次 |
| `g24_transmit_hop_process` | 204-255 | 跳频处理：重传失败时切换 RF 通道 |
| `g24_ackpayload_parse` | 176-184 | 解析收到的 ACK payload |

### 接收流程

`program/g24_protocol_stack/24g.prog`：

| 函数 | 行号 | 说明 |
|------|------|------|
| `g24_receive_packet` | 20-112 | 接收数据包：同步字检测、去白化、FEC 解码、CRC 校验、提取 payload 和 RSSI |
| `g24_read_len_pid_crc` | 345-362 | 从收到的包中读取 payload 长度、PID、CRC |

`program/g24_protocol_stack/24g_receiver.prog`：

| 函数 | 行号 | 说明 |
|------|------|------|
| `g24_receive_transmit_ack` | 107-126 | 收到包后回复 ACK（含 payload），取决于 `bit_ack_24g` 标志 |
| `g24_receive_packet_parse` | 24-33 | 解析收到数据包的内容到 `mem_24g_rxdata_temp` |
| `g24_transmit_ack` | 111-126 | ACK 回复：设置 data_type、读取 len/pid/crc、调用 `g24_ackpayload_prep` 准备 ACK payload、发送 |
| `g24_ackpayload_prep` | 12-18 | 准备 ACK payload（清零 datalen，调用回调函数 `mem_cb_24g_ackpayload`） |
| `g24_receive_packet_start` | 21-23 | 接收并回复 ACK 的总入口 |

### 接收端模式管理

`program/g24_protocol_stack/24g_receiver.prog`：

| 函数                          | 行号                        | 说明                  |
| --------------------------- | ------------------------- | ------------------- |
| `g24_work_init`             | 250-265                   | 工作模式初始化             |
| `g24_work_mode_start`       | 276-281                   | 工作模式接收循环            |
| `g24_switch_work_mode`      | 268-273                   | 切换到工作模式             |
| `g24_bind_mode_enable`      | `24g_pair.prog` 行 184-187 | 启用对码模式              |
| `g24_bind_init`             | `24g_pair.prog` 行 189-200 | 对码模式初始化             |
| `g24_bind_mode_auto`        | 294-307                   | 对码模式自动循环            |
| `g24_search_mode_enable`    | 531-535                   | 切换到搜索模式             |
| `g24_search_mode_auto`      | 341-378                   | 搜索模式自动循环（多地址轮询）     |
| `g24_nodata_process`        | 519-535                   | 无数据超时处理（切回搜索模式）     |
| `g24_mode_switch_bind_work` | 147-160                   | BIND/WORK 模式时间片轮换   |
| `g24_mode_B_S_switch_init`  | 180-194                   | 绑定/搜索模式切换初始化（自动配对用） |

---

## 完整调用链总结

### 发射端（鼠标/键盘）

```
app_mouse.prog: mouse_24g_start_pair_mode (4448)
  └── 24g_pair.prog: g24_pair_start (21)
        ├── 设置 STATE_24G_PAIR 到 mem_24g_conn_sm
        ├── 初始化对码超时
        ├── 24g_pair.prog: g24_pair_sm_reinit (108) — 决定起始状态
        └── 24g_pair.prog: g24_pair_init_param (33) — 设置通道/功率/地址
              └── 24g.prog: g24_update_addr_and_synccrc8 (392)

每次发射周期:
24g_transmitter.prog: g24_transmit_dispatch (29)
  └── 检测 STATE_24G_PAIR → 24g_pair.prog: g24_pair_dispatch (43)
        ├── 按状态分发到 g24_pair_sm_1/2/3/4
        ├── 组装 txbuf → g24_pair_sm_common (86)
        │     ├── 24g.prog: g24_transmit_prep (282)
        │     ├── 24g_transmitter.prog: g24_transmit_receive_ack (142)
        │     │     ├── 24g.prog: g24_transmit (179) — RF 发送
        │     │     └── 24g.prog: g24_receive_packet (20) — RF 接收 ACK
        │     ├── 24g_transmitter.prog: g24_ackpayload_parse (176)
        │     └── 状态机推进: g24_pair_sm_N_waiting_ack
        └── 成功后: STATE_24G_PAIRING_SUCCESS
              ├── 通知 UI: ui_ipc_send_event (BT_EVT_24G_PAIRING_COMPLETE)
              ├── 更新通信地址: g24_update_addr_and_synccrc8
              └── g24_pair_exit: 清除 STATE_24G_PAIR
```

### 接收端（Dongle）

```
app_dongle.prog: dongle_init (3) — 注册回调
app_dongle.prog: dongle_default_init (17)
  ├── 24g_receiver.prog: g24_receive_init
  ├── 24g_pair.prog: g24_pair_param_init
  └── 24g.prog: g24_chmap_param_init

app_dongle.prog: dongle_dispatch (65) — 模式分发
  └── dongle_pc_bind (146) — PC 对码循环
        └── 24g_receiver.prog: g24_bind_mode_auto (294)
              ├── 24g_pair.prog: g24_bind_init (189)
              │     ├── 设置 DONGLE_BIND 模式
              │     ├── 初始化 bind_step/disable 标志
              │     ├── 保存 mem_24g_transmitter_addr
              │     ├── 启用 ACK payload
              │     └── 24g_pair.prog: g24_pair_init_param (33)
              └── 24g_pair.prog: g24_bind_data_process (211)
                    ├── 24g.prog: g24_receive_packet (20) — RF 接收
                    ├── 24g_pair.prog: g24_bind_data_parse (230)
                    │     ├── DATATYPE_BIND → g24_bind_first_step
                    │     ├── DATATYPE_CONFIG → g24_bind_second_step
                    │     ├── DATATYPE_OK → g24_bind_third_step
                    │     └── DATATYPE_ATTEMP → g24_data_attemp
                    └── 24g_receiver.prog: g24_transmit_ack (111) — 回复 ACK
                          └── 24g.prog: g24_transmit (179) — RF 发送 ACK

对码成功后:
  └── 24g_receiver.prog: g24_switch_work_mode (268)
        └── 切到 DONGLE_SEARCH → DONGLE_WORK
```

---

## 对码参数配置

定义于 `format/g24_protocol_stack/24g_pair.format` (行 3-52)：

| 参数                           | 大小      | 说明                      |
| ---------------------------- | ------- | ----------------------- |
| `mem_24g_pair_addr`          | 4 bytes | 对码使用的 RF 地址             |
| `mem_24g_pair_ch`            | 1 byte  | 对码使用的 RF 通道 (默认 0x4E)   |
| `mem_24g_pair_tx_power`      | 1 byte  | 对码发射功率                  |
| `mem_24g_pair_sm`            | 1 byte  | 对码状态机当前状态               |
| `mem_24g_pair_no_ack`        | 1 byte  | 无 ACK 标志                |
| `mem_24g_pair_timeout_init`  | 2 bytes | 对码超时初始值                 |
| `mem_24g_pair_timeout`       | 2 bytes | 对码超时计数器                 |
| `mem_24g_pair_count`         | 2 bytes | 对码尝试次数                  |
| `mem_24g_device1_bind_step`  | 1 byte  | 鼠标绑定步骤 (0→1→2→成功)       |
| `mem_24g_device2_bind_step`  | 1 byte  | 键盘绑定步骤 (0→1→2→成功)       |
| `mem_24g_bind_device_status` | 1 byte  | 绑定状态 (bit0=鼠标, bit1=键盘) |
| `mem_24g_bind_payload`       | 8 bytes | ACK payload 缓冲区         |
| `mem_24g_pair_switch`        | 1 byte  | 快速配对模式开关                |
| `mem_24g_bind_mode_continue` | 1 byte  | 对码模式持续时间控制 (约 400ms/单位) |

---

## 重连协议机制

### 概述

对码完成后，发射端（鼠标/键盘）和接收端（Dongle）互知对方地址。当发射端从睡眠唤醒或断线后需要恢复通信时，不重复四步握手，而是通过发送 `DATATYPE_ATTEMP (0xFF)` 包发起重连。重连本质上是**轻量版的 Step 1**，目标是让 Dongle 重新识别设备并恢复数据通信。

### 核心文件

| 文件 | 说明 |
|------|------|
| `program/g24_protocol_stack/24g_reconn.prog` | 重连全部逻辑：启动、调度、成功/失败处理（发射端 + 接收端） |
| `format/g24_protocol_stack/24g_reconn.format` | 重连数据结构定义 |
| `program/g24_protocol_stack/24g_transmitter.prog` | 发射调度分发：`STATE_24G_RECONN` → `g24_reconn_dispatch` |
| `format/g24_protocol_stack/24g_transmitter.format` | 连接状态机定义（含 `STATE_24G_RECONN`） |
| `program/g24_protocol_stack/24g_receiver.prog` | 接收端搜索模式，处理 ATTEMP 回调 |

### 关键数据结构

定义于 `format/g24_protocol_stack/24g_reconn.format`：

| 变量 | 大小 | 说明 |
|------|------|------|
| `mem_24g_reconn_timeout_init` | 2 bytes | 重连超时初始值 |
| `mem_24g_reconn_timeout` | 2 bytes | 重连超时计数器 |
| `mem_24g_reconn_count` | 2 bytes | 重连调度计数器（低 9 位控制发送节奏） |
| `mem_24g_reconn_type` | 1 byte | 重连目标类型，决定地址轮换策略 |
| `mem_24g_fast_conn_enable` | 1 byte | 是否启用快速连接地址 |
| `mem_24g_fast_conn_addr` | 4 bytes | 快速连接的临时地址 |
| `mem_24g_receiver_addr` | 4 bytes | Dongle 的专属地址（对码 Step 2 获取） |
| `mem_24g_device_addr` | 4 bytes | 自身设备地址（发射端地址） |

#### 重连类型 (`mem_24g_reconn_type`)

| 值 | 常量 | 地址轮换策略 |
|----|------|-------------|
| 0 | `DEFAULT_24G_DEVICE` | 只用 `receiver_addr` |
| 1 | `FAST_CONN_AND_RECEIVER` | `fast_conn_addr` ↔ `receiver_addr` 交替 |
| 2 | `FAST_CONN_AND_3_0_ADDR` | `fast_conn_addr` ↔ `device_addr` (3.0) 交替 |
| 3 | `RECEIVER_AND_3_0_ADDR` | `receiver_addr` ↔ `device_addr` (3.0) 交替 |
| 4 | `PAIR_AND_3_0_ADDR` | `pair_addr` (对码广播) ↔ `device_addr` (3.0) 交替 |

连接状态机 (`mem_24g_conn_sm`，定义于 `24g_transmitter.format`)：

| 位 | 常量 | 说明 |
|----|------|------|
| bit0 | `STATE_24G_PAIR` | 对码模式 |
| bit1 | `STATE_24G_RECONN` | 重连模式 |
| — | (均为 0) | 停止/正常工作模式 |

### 发射端重连流程

#### 阶段一：重连启动 — `g24_reconn_start`

`program/g24_protocol_stack/24g_reconn.prog` (行 10-50)：

1. 设置 `mark_24g` 标志，启用 24G 调度
2. 将 `mem_24g_conn_sm` 从对码或停止状态切换为 `STATE_24G_RECONN`——后续 `g24_transmit_dispatch` 每次发射调度都会检测此标志并跳转到 `g24_reconn_dispatch`
3. 检查 `mem_24g_fast_conn_enable`：若为 0（快速连接未启用），直接使用 receiver 地址
4. 根据 `mem_24g_reconn_type` 选择首次尝试的目标地址，配置对应的 RF 参数：

```
type=1,2 → g24_reconn_fast_conn:      对码功率 + fast_conn_addr + fast_conn_temp_ch
type=3,4 → g24_reconn_device_3_0_addr: 3dB 功率 + device_addr + device_temp_ch
default  → g24_reconn_receiver_addr:   3dB 功率 + receiver_addr + receiver_temp_ch
```

每个分支都调用 `g24_ch_syncword_crc8_init`，将目标地址写入 RF 硬件并计算同步字和 CRC8 初值。

#### 阶段二：速率控制 — `g24_reconn_dispatch`

`program/g24_protocol_stack/24g_reconn.prog` (行 61-70)：

重连不是每个周期都发送 ATTEMP 包，而是采用**突发 + 退避**的节奏：

```
reconn_count & 0x01F8 == 0  → 执行调度（真正发送 ATTEMP）
否则                         → 仅递增计数，跳过
```

其中 `0x01F8` 的二进制为 `0000 0001 1111 1000`，掩码提取 bit3-bit8。`count % 512 < 8` 时触发，即每 512 个周期发动一次 8 连发，中间 504 个周期静默。则每 512个周期 密集尝试 8 次。

#### 阶段三：发送与 ACK 验证 — `g24_reconn_dispatch_next`

`program/g24_protocol_stack/24g_reconn.prog` (行 72-101)：

**3.1 发送 ATTEMP 包：**

调用 `g24_reconn_data_prep` 组装数据包 → `g24_transmit_receive_ack` 发射并等待 ACK。ATTEMP 包结构（6 字节）：

```
[0] = 0xFF              ← DATATYPE_ATTEMP
[1] = 设备类型            ← TYPE_MS 或 TYPE_KB（低 3 位）
[2-5]                   ← 设备地址 (mem_24g_device_addr)
```

**3.2 同步/ACK 失败：**

`sync` 失败（未检测到同步字）或 `user3` 失败（ACK 超时/CRC 错）→ 跳 `g24_reconn_device_fail`，根据 `mem_24g_reconn_type` 切换地址后重试。

**3.3 ACK 成功，解析并验证：**

调用 `g24_ackpayload_parse` 解析 ACK payload，然后执行两级地址校验：

```
第一级：ACK 地址 == mem_24g_pair_addr（对码广播地址）？
  → 是：Dongle 还在 BIND 模式，双方无绑定关系
        直接跳 g24_pair_start 走完整四步握手对码

第二级：ACK 地址 == mem_24g_device_addr（自身设备地址）？
  → 是：Dongle 已识别本设备，用的是专属地址回复
        调用 g24_store_receiver_addr 保存 Dongle 地址
        进入 g24_reconn_dispatch_end 宣告重连成功
```

**3.4 重连成功收尾 — `g24_reconn_dispatch_end`：**

- 清除 `STATE_24G_RECONN`，退出重连模式
- 清零 `mem_24g_pair_timeout`、`mem_24g_reconn_count`、`mem_24g_reconn_timeout`
- 清空发送缓冲区 (`g24_txbuf_clear`)
- 发送 `BT_EVT_24G_ATTEMPT_SUCCESS` 事件通知 UI 层

#### 阶段四：失败轮换 — `g24_reconn_device_fail`

`program/g24_protocol_stack/24g_reconn.prog` (行 111-141)：

失败时递增 `mem_24g_reconn_count`，取 bit8 位判断奇偶，根据 type 在两种地址之间切换：

| type | bit8=0（偶数轮） | bit8=1（奇数轮） |
|------|------------------|------------------|
| FAST_CONN_AND_RECEIVER | receiver_addr | fast_conn_addr |
| FAST_CONN_AND_3_0_ADDR | 3.0 device_addr | fast_conn_addr |
| RECEIVER_AND_3_0_ADDR | 3.0 device_addr | receiver_addr |
| PAIR_AND_3_0_ADDR | pair_addr（走对码） | 3.0 device_addr |

### 接收端 ATTEMP 处理

接收端在三个场景下会收到 `DATATYPE_ATTEMP` 包：

**1. 对码阶段 — 搜索模式 — `g24_search_mode_auto`**

Dongle 对码成功后进入 `DONGLE_SEARCH` 模式（通过 `g24_search_mode_enable`），在已绑定的设备地址之间轮询监听。收到数据后按类型分发：

`program/g24_protocol_stack/24g_receiver.prog` (行 38-41)：
```
DATATYPE_DEVICE1 → 鼠标数据回调
DATATYPE_DEVICE2 → 键盘数据回调
DATATYPE_ATTEMP  → g24_data_receive_attemp → g24_data_attemp
```

**2. ATTEMP 设备地址保存 — `g24_data_attemp`**

`program/g24_protocol_stack/24g_reconn.prog` (行 151-171)：

```
从 rxdata_temp[3] 判断设备类型:
  DEVICE1 → g24_data_attemp_device1:
    提取 rxdata_temp[4-7] → mem_24g_transmitter_addr（保存发射端地址）
    提取 rxdata_temp[4-7] → mem_24g_device1_addr（保存为鼠标地址）
  DEVICE2 → g24_data_attemp_device2:
    提取 rxdata_temp[4-7] → mem_24g_transmitter_addr
    提取 rxdata_temp[4-7] → mem_24g_device2_addr（保存为键盘地址）
```

前提是该设备尚未绑定 (`mem_24g_deviceX_bind_disable == 0`)。

**3. 带中止标记的 ATTEMP — `g24_data_attemp_deviceX_abort`**

若收到带 `bit_abort` 标志的 DEVICE1 包，Dongle 会强制清除 `mem_24g_device1_bind_disable`，解除绑定并重新保存地址。这用于发射端主动触发解绑重连的场景。

### Dongle 地址状态与重连行为的关系

Dongle 在不同工作模式下使用的 RF 地址不同，这直接决定了重连的结果：

| Dongle 状态 | 监听地址 | 发射端发往 receiver_addr | 结果 |
|-------------|---------|--------------------------|------|
| BIND 模式（未绑定/刚上电） | `pair_addr`（对码广播） | receiver_addr（不对） | ACK 地址 == pair_addr → 触发完整对码 |
| SEARCH 模式（已绑定） | 设备专属地址（device1/device2 轮询） | receiver_addr（匹配） | ACK 地址 == device_addr → 重连成功 |
| WORK 模式（正常通信） | 当前活跃设备地址 | receiver_addr（匹配） | 正常数据通信 |

关键结论：只要 Dongle 不掉电（不掉绑定状态），发射端唤醒后直接用 `receiver_addr` 即可重连成功。`pair_addr` 的校验分支主要兜底 Dongle 丢失绑定信息的边界场景。

### 应用层触发入口

`program/app_mouse.prog` 中的重连触发路径：

**上电唤醒：**
```
mouse_wakeup_from_poweron
  → mouse4_0_check_reconn_target
    → 设置 mem_mouse_le_reconnect_flag = 1
    → 配置直连定时器
    → 发起重连
```

**直连超时：**
```
mouse_check_direct_timeout
  → mem_reconn_times 耗尽
  → 未启用 BT_RECONNECT_TIMEOUT_ENTER_DISCOVERY
  → 未启用 24G_FIRST_AUTO_PAIRING
  → mouse_app_enter_hibernate（进入休眠）
```

**DPI 按键长按：**
```
mouse_ready_reconnection_by_dpi_button
  → mouse_start_reconnect_device
    → 根据 device_flag 选择目标 → 发起重连
```

**BLE 配对完成后存储重连信息：**
```
BT_EVT_LE_PAIRING_COMPLETE
  → app_ble_store_reconn_info
  → mouse_24g_first_store_reconn_info
```

**定时器调度链：**
```
mouse_main_timer
  → g24_reconn_timeout_timer          // 重连超时检查
  → mouse_check_reconnect_delay_long_sleep  // 重连失败延迟进休眠
```

### 完整重连调用链

```
发射端（鼠标）：
  app_mouse.prog: 触发重连（唤醒/按键/超时）
    └── 设置 STATE_24G_RECONN
        └── 24g_transmitter.prog: g24_transmit_dispatch
              └── 24g_reconn.prog: g24_reconn_dispatch（速率控制）
                    ├── g24_reconn_data_prep（组装 ATTEMP 包）
                    ├── 24g.prog: g24_transmit（RF 发射）
                    ├── 24g_transmitter.prog: g24_transmit_receive_ack（等待 ACK）
                    ├── 24g_transmitter.prog: g24_ackpayload_parse（解析 ACK）
                    ├── 地址校验 → 成功/失败/退化为对码
                    └── g24_reconn_dispatch_end（清理 + 通知上层）

接收端（Dongle）：
  SEARCH 模式 / BIND 模式
    └── 24g.prog: g24_receive_packet（RF 接收）
        └── 24g_receiver.prog: g24_receive_packet_parse
              ├── DATATYPE_ATTEMP → g24_reconn.prog: g24_data_attemp
              │     └── 保存发射端地址到 device1/device2_addr
              └── 24g_receiver.prog: g24_transmit_ack（回复 ACK）
```
