# 24G 对码 — 底层 RF 收发与完整调用链

## 底层 RF 收发支撑

### 发射端 RF 层

`program/g24_protocol_stack/24g.prog`：

| 函数 | 行号 | 说明 |
|------|------|------|
| `g24_transmit_prep` | 282-322 | 准备发送载荷：HEC、PDU 控制字段、数据类型、payload 数据拼接、RSSI hex |
| `g24_lr_transmit_prep` | 324-343 | LR (Long Range) 模式的载荷准备 |
| `g24_transmit` | 179-274 | 发送数据包：前导码、访问地址、白化、FEC 编码、CRC、RF 调制发射 |

`program/g24_protocol_stack/24g_transmitter.prog`：

| 函数 | 行号 | 说明 |
|------|------|------|
| `g24_transmit_receive_ack` | 142-156 | 发送后立即切换到 RX 模式等待 ACK |
| `g24_transmit_process` | 113-140 | 发射主流程：发送 → 等 ACK → 解析 ACK payload → 处理重传 |
| `g24_retransmit` | 158-174 | 重传机制：ACK 超时或失败时重试，最多 N 次 |
| `g24_transmit_hop_process` | 204-255 | 跳频处理：重传失败时切换 RF 通道 |
| `g24_ackpayload_parse` | 176-184 | 解析收到的 ACK payload |

### 接收端 RF 层

`program/g24_protocol_stack/24g.prog`：

| 函数 | 行号 | 说明 |
|------|------|------|
| `g24_receive_packet` | 20-112 | 接收数据包：同步字检测、去白化、FEC 解码、CRC 校验、提取 payload 和 RSSI |
| `g24_read_len_pid_crc` | 345-362 | 从收到的包中读取 payload 长度、PID、CRC |

`program/g24_protocol_stack/24g_receiver.prog`：

| 函数 | 行号 | 说明 |
|------|------|------|
| `g24_receive_transmit_ack` | 107-126 | 收到包后回复 ACK（含 payload），取决于 `bit_ack_24g` 标志 |
| `g24_receive_packet_parse` | 24-33 | 解析收到数据包的内容到 `mem_24g_rxdata_temp` |
| `g24_transmit_ack` | 111-126 | ACK 回复：设置 data_type、读取 len/pid/crc、调用 `g24_ackpayload_prep` 准备 ACK payload、发送 |
| `g24_ackpayload_prep` | 12-18 | 准备 ACK payload（清零 datalen，调用回调函数 `mem_cb_24g_ackpayload`） |
| `g24_receive_packet_start` | 21-23 | 接收并回复 ACK 的总入口 |

### 接收端模式管理

`program/g24_protocol_stack/24g_receiver.prog`：

| 函数 | 行号 | 说明 |
|------|------|------|
| `g24_work_init` | 250-265 | 工作模式初始化 |
| `g24_work_mode_start` | 276-281 | 工作模式接收循环 |
| `g24_switch_work_mode` | 268-273 | 切换到工作模式 |
| `g24_bind_mode_enable` | `24g_pair.prog` 行 184-187 | 启用对码模式 |
| `g24_bind_init` | `24g_pair.prog` 行 189-200 | 对码模式初始化 |
| `g24_bind_mode_auto` | 294-307 | 对码模式自动循环 |
| `g24_search_mode_enable` | 531-535 | 切换到搜索模式 |
| `g24_search_mode_auto` | 341-378 | 搜索模式自动循环（多地址轮询） |
| `g24_nodata_process` | 519-535 | 无数据超时处理（切回搜索模式） |
| `g24_mode_switch_bind_work` | 147-160 | BIND/WORK 模式时间片轮换 |
| `g24_mode_B_S_switch_init` | 180-194 | 绑定/搜索模式切换初始化（自动配对用） |

### 底层 RF 收发的关键细节

#### Control 字节结构

`g24_transmit_prep_pdu`（`24g.prog` 行 297-306）：

```
Control = (datalen << 3) | (PID << 1) | (NO_ACK << 0)
          └── 5 bits   └── 2 bits   └── 1 bit
```

| 字段 | 位 | 说明 |
|------|-------|------|
| payload_len | bit 7:3 | payload 长度（字节数） |
| PID | bit 2:1 | 包 ID（0-3，用于去重） |
| NO_ACK | bit 0 | 不要求 ACK（1=不需要 ACK） |

对码时：datalen=7, Control = 7<<3 | PID<<1 | 0 = 0x38
ACK 时：datalen=8, Control = 8<<3 | PID<<1 | 0 = 0x40
重连时：datalen=6, Control = 6<<3 | PID<<1 | 0 = 0x30

#### Preamble (前导码)

24G 使用自定义 FSK 收发器（非标准 BLE），Preamble 为 **5 字节（40 bits）**。而 BLE 的 Preamble 仅为 1 字节（8 bits）——这是因为 BLE 硬件专门针对短前导做了优化，而此项目的通用 FSK 收发器需要更长的前导码来做比特同步和信号建立。

`g24_transmit`（`24g.prog` 行 179-274）在发送 payload 之前先发送 40 bits 的 Preamble，然后依次是 4 字节 Access Address、1 字节 HEC、PDU、和 3 字节 CRC。

#### HEC (Header Error Check)

`g24_update_addr_and_synccrc8`（`24g.prog` 行 392-410）：

1. 将 4 字节地址相加得到 syncword
2. syncword 的低字节 + 高字节 = crc8（即 HEC）

HEC 是对 Access Address 的校验，接收方用它来验证地址是否正确收到了。

#### `g24_transmit_ack` 函数

`program/g24_protocol_stack/24g_receiver.prog` (行 111-126)：

```asm
g24_transmit_ack:
    fetch 1,mem_24g_rxbuf              ; 读收到的包的第一个字节
    store 1,mem_24g_data_type          ; 设为发射时的 data_type（rxbuf[0] 回传）
    call g24_read_len_pid_crc          ; 解析长度/PID/CRC
    fetch 1,mem_24g_rxbuf+1            ; 读 Control 字节
    isolate1 bit_ack_24g,pdata         ; 检查 NO_ACK 位
    branch g24tx_no_ack,true           ; NO_ACK=1 → 不回复 ACK
g24tx_with_ack:
    call g24_ackpayload_prep           ; 准备 ACK payload（8 字节）
    call g24_transmit_prep             ; 组 PDU
    call g24_transmit                  ; RF 发送
g24tx_no_ack:
    branch g24_end_of_packet
```

收到每个包都会检查 Control 字节的 bit0（NO_ACK）。如果置位则跳过 ACK 回复（重连包就是这种情况），如果未置位则准备 ACK payload 并发送。

---

## 完整调用链

### 发射端（鼠标/键盘）对码调用链

```
app_mouse.prog: mouse_24g_start_pair_mode (4448)
  └── 24g_pair.prog: g24_pair_start (21)
        ├── 设置 STATE_24G_PAIR 到 mem_24g_conn_sm
        ├── 初始化对码超时
        ├── 24g_pair.prog: g24_pair_sm_reinit (108) — 决定起始状态
        └── 24g_pair.prog: g24_pair_init_param (33) — 设置通道/功率/地址
              └── 24g.prog: g24_update_addr_and_synccrc8 (392)

每次发射周期:
24g_transmitter.prog: g24_transmit_dispatch (29)
  └── 检测 STATE_24G_PAIR → 24g_pair.prog: g24_pair_dispatch (43)
        ├── 按状态分发到 g24_pair_sm_1/2/3/4
        ├── 组装 txbuf → g24_pair_sm_common (86)
        │     ├── 24g.prog: g24_transmit_prep (282)
        │     ├── 24g_transmitter.prog: g24_transmit_receive_ack (142)
        │     │     ├── 24g.prog: g24_transmit (179) — RF 发送
        │     │     └── 24g.prog: g24_receive_packet (20) — RF 接收 ACK
        │     ├── 24g_transmitter.prog: g24_ackpayload_parse (176)
        │     └── 状态机推进: g24_pair_sm_N_waiting_ack
        └── 成功后: STATE_24G_PAIRING_SUCCESS
              ├── 通知 UI: ui_ipc_send_event (BT_EVT_24G_PAIRING_COMPLETE)
              ├── 更新通信地址: g24_update_addr_and_synccrc8
              └── g24_pair_exit: 清除 STATE_24G_PAIR
```

### 接收端（Dongle）对码调用链

```
app_dongle.prog: dongle_init (3) — 注册回调
app_dongle.prog: dongle_default_init (17)
  ├── 24g_receiver.prog: g24_receive_init
  ├── 24g_pair.prog: g24_pair_param_init
  └── 24g.prog: g24_chmap_param_init

app_dongle.prog: dongle_dispatch (65) — 模式分发
  └── dongle_pc_bind (146) — PC 对码循环
        └── 24g_receiver.prog: g24_bind_mode_auto (294)
              ├── 24g_pair.prog: g24_bind_init (189)
              │     ├── 设置 DONGLE_BIND 模式
              │     ├── 初始化 bind_step/disable 标志
              │     ├── 保存 mem_24g_transmitter_addr
              │     ├── 启用 ACK payload
              │     └── 24g_pair.prog: g24_pair_init_param (33)
              └── 24g_pair.prog: g24_bind_data_process (211)
                    ├── 24g.prog: g24_receive_packet (20) — RF 接收
                    ├── 24g_pair.prog: g24_bind_data_parse (230)
                    │     ├── DATATYPE_BIND → g24_bind_first_step
                    │     ├── DATATYPE_CONFIG → g24_bind_second_step
                    │     ├── DATATYPE_OK → g24_bind_third_step
                    │     └── DATATYPE_ATTEMP → g24_data_attemp
                    └── 24g_receiver.prog: g24_transmit_ack (111) — 回复 ACK
                          └── 24g.prog: g24_transmit (179) — RF 发送 ACK

对码成功后:
  └── 24g_receiver.prog: g24_switch_work_mode (268)
        └── 切到 DONGLE_SEARCH → DONGLE_WORK
```

### 发射端重连调用链

```
发射端（鼠标）：
  app_mouse.prog: 触发重连（唤醒/按键/超时）
    └── 设置 STATE_24G_RECONN
        └── 24g_transmitter.prog: g24_transmit_dispatch
              └── 24g_reconn.prog: g24_reconn_dispatch（速率控制）
                    ├── g24_reconn_data_prep（组装 ATTEMP 包）
                    ├── 24g.prog: g24_transmit（RF 发射）
                    ├── 24g_transmitter.prog: g24_transmit_receive_ack（等待 ACK）
                    ├── 24g_transmitter.prog: g24_ackpayload_parse（解析 ACK）
                    ├── 地址校验 → 成功/失败/退化为对码
                    └── g24_reconn_dispatch_end（清理 + 通知上层）

接收端（Dongle）：
  SEARCH 模式 / BIND 模式
    └── 24g.prog: g24_receive_packet（RF 接收）
        └── 24g_receiver.prog: g24_receive_packet_parse
              ├── DATATYPE_ATTEMP → g24_reconn.prog: g24_data_attemp
              │     └── 保存发射端地址到 device1/device2_addr
              └── 24g_receiver.prog: g24_transmit_ack（回复 ACK）
```

---

## 关键函数速查表

### 对码状态机函数（24g_pair.prog）

| 函数 | 行号 | 说明 |
|------|------|------|
| `g24_pair_start` | 21-39 | 对码启动入口 |
| `g24_pair_init_param` | 33-39 | 对码参数初始化（通道、功率、地址） |
| `g24_pair_dispatch` | 43-58 | 状态机分发器（速率限制：每 256 次计数执行一次） |
| `g24_pair_sm_1` | 60-61 | Step 1 ATTEMP：填充 0xFF |
| `g24_pair_sm_2` | 64-71 | Step 2 BIND：填充 0xAA，pair_switch=1 时切地址 |
| `g24_pair_sm_3` | 73-79 | Step 3 CONFIG：填充 0x55 |
| `g24_pair_sm_4` | 81-82 | Step 4 OK：填充 0x22 |
| `g24_pair_sm_common` | 86-106 | 通用流程：发送→等 ACK→解析→分发 |
| `g24_pair_sm_reinit` | 108-113 | 状态机初始化 |

### 对码 ACK 等待函数（24g_pair.prog）

| 函数 | 行号 | 说明 |
|------|------|------|
| `g24_pair_sm_1_waiting_ack` | 115-117 | Step 1 ACK：推进到 STATE_2 |
| `g24_pair_sm_2_waiting_ack` | 119-126 | Step 2 ACK：提取 Dongle 地址，推进到 STATE_3 |
| `g24_pair_sm_3_waiting_ack` | 128-137 | Step 3 ACK：验证地址，推进到 STATE_4 |
| `g24_pair_sm_4_waiting_ack` | 139-158 | Step 4 ACK：对码成功，通知 UI，退出 |

### Dongle 对码处理函数（24g_pair.prog）

| 函数 | 行号 | 说明 |
|------|------|------|
| `g24_bind_mode_enable` | 184-187 | 启用对码模式 |
| `g24_bind_init` | 189-200 | 对码初始化 |
| `g24_bind_data_process` | 211-224 | 核心接收-解析-ACK 循环 |
| `g24_bind_data_parse` | 230-241 | 按数据类型分发 |
| `g24_bind_first_step` | 244-268 | Step 2 接收：准备 LAP ACK payload |
| `g24_bind_second_step` | 271-313 | Step 3 接收：地址校验 + 双向确认 |
| `g24_bind_third_step` | 316-361 | Step 4 接收：最终确认 + 标记绑定 |
| `g24_bind_ackpayload_prep` | 203-208 | ACK payload 组装 |

### 重连函数（24g_reconn.prog）

| 函数 | 行号 | 说明 |
|------|------|------|
| `g24_reconn_start` | 10-50 | 重连启动 |
| `g24_reconn_dispatch` | 61-109 | 重连调度（速率控制 + 发送 + 验证） |
| `g24_reconn_device_fail` | 111-141 | 重连失败 → 地址轮换 |
| `g24_data_attemp` | 151-171 | ATTEMP 包处理（对码 Step 1 / 重连共用） |
| `g24_reconn_data_prep` | 102-109 | 组装重连 ATTEMP 包 |

### 底层 RF 函数（24g.prog）

| 函数 | 行号 | 说明 |
|------|------|------|
| `g24_receive_packet` | 20-112 | RF 接收 |
| `g24_transmit` | 179-274 | RF 发射 |
| `g24_transmit_prep` | 282-322 | PDU 组装 |
| `g24_transmit_prep_pdu` | 297-306 | Control 字节组装 |
| `g24_read_len_pid_crc` | 345-362 | 解析收到的 Control 字节 |
| `g24_update_addr_and_synccrc8` | 392-410 | 更新地址 + 计算同步字 + HEC |
| `g24_syncword_crc8` | 396-409 | 同步字和 CRC8 计算 |

### 模式管理函数（24g_receiver.prog）

| 函数 | 行号 | 说明 |
|------|------|------|
| `g24_work_init` | 250-265 | 工作模式初始化 |
| `g24_switch_work_mode` | 268-273 | 切工作模式 |
| `g24_search_mode_auto` | 341-378 | 搜索模式自动轮询 |
| `g24_search_mode_enable` | 531-535 | 切搜索模式 |
| `g24_nodata_process` | 519-535 | 无数据超时处理 |
| `g24_mode_switch_bind_work` | 147-160 | BIND/WORK 时间片轮换 |
```

### 应用层函数（app_mouse.prog / app_dongle.prog）

| 函数 | 文件 | 行号 | 说明 |
|------|------|------|------|
| `mouse_24g_start_pair_mode` | app_mouse.prog | 4448 | 鼠标触发对码 |
| `mouse_enable_function_flag` | app_mouse.prog | 3997 | 设置功能标志位 |
| `mouse_disable_function_flag` | app_mouse.prog | 4003 | 清除功能标志位 |
| `dongle_init` | app_dongle.prog | 3 | Dongle 初始化（注册回调） |
| `dongle_default_init` | app_dongle.prog | 17 | Dongle 硬件初始化 |
| `dongle_dispatch` | app_dongle.prog | 65 | Dongle 模式分发 |
| `dongle_pc_bind` | app_dongle.prog | 146 | PC 对码主循环 |
| `dongle_ackpayload_prep` | app_dongle.prog | 335 | ACK payload 回调 |
