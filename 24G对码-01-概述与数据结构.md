# 24G 对码 — 概述与数据结构

## 概述

24G 对码是鼠标/键盘（发射端）与 USB Dongle（接收端）之间建立配对关系的过程，采用 4 步握手机制完成设备识别和地址交换。对码成功后双方互知对方地址，后续通过重连流程恢复数据通信。

---

## 文件结构

### 核心对码文件

| 文件 | 说明 |
|------|------|
| `program/g24_protocol_stack/24g_pair.prog` | 对码状态机和握手流程全部实现（发射端 + 接收端） |
| `format/g24_protocol_stack/24g_pair.format` | 对码数据结构定义（状态常量、内存分配） |
| `program/g24_protocol_stack/24g.prog` | 底层 RF 收发、载荷组包、ACK 机制 |
| `format/g24_protocol_stack/24g.format` | 基础 RF 数据结构（地址、通道映射、缓冲区） |
| `program/g24_protocol_stack/24g_transmitter.prog` | 发射调度、ACK 等待、重传跳频 |
| `format/g24_protocol_stack/24g_transmitter.format` | 发射端数据结构（连接状态机、重试计数等） |
| `program/g24_protocol_stack/24g_receiver.prog` | 接收调度、模式切换（BIND → SEARCH → WORK） |
| `format/g24_protocol_stack/24g_receiver.format` | 接收端数据结构（工作模式、绑定状态、ACK payload） |
| `program/g24_protocol_stack/24g_reconn.prog` | 重连流程（对码成功后恢复通信） |
| `format/g24_protocol_stack/24g_reconn.format` | 重连数据结构 |

### 应用层入口文件

| 文件 | 说明 |
|------|------|
| `program/app_mouse.prog` | 鼠标端：触发 `g24_pair_start` 进入对码 |
| `program/app_dongle.prog` | Dongle 端：主循环调用 `g24_bind_mode_auto` 处理对码 |

---

## 关键数据结构

### 对码状态机 (`mem_24g_pair_sm`)

定义于 `format/g24_protocol_stack/24g_pair.format` (行 28-38)：

| 值 | 常量 | 说明 |
|----|------|------|
| 0x00 | STATE_24G_PAIRING_NULL | 初始/空闲 |
| 0x01 | STATE_24G_PAIRING_1 | 步骤 1：发送 ATTEMP |
| 0x02 | STATE_24G_PAIRING_2 | 步骤 2：发送 BIND |
| 0x03 | STATE_24G_PAIRING_3 | 步骤 3：发送 CONFIG |
| 0x04 | STATE_24G_PAIRING_4 | 步骤 4：发送 OK |
| 0x11 | STATE_24G_PAIRING_1_WAITING_ACK | 步骤 1 等待 ACK |
| 0x12 | STATE_24G_PAIRING_2_WAITING_ACK | 步骤 2 等待 ACK |
| 0x13 | STATE_24G_PAIRING_3_WAITING_ACK | 步骤 3 等待 ACK |
| 0x14 | STATE_24G_PAIRING_4_WAITING_ACK | 步骤 4 等待 ACK |
| 0xFF | STATE_24G_PAIRING_SUCCESS | 对码成功 |

### 连接状态机 (`mem_24g_conn_sm`)

定义于 `format/g24_protocol_stack/24g_transmitter.format` (行 40-45)：

| 值 | 常量 | 说明 |
|----|------|------|
| 0 | STATE_24G_STOP | 停止 |
| 0 | STATE_24G_START | 启动 |
| 1 | STATE_24G_PAIR | 对码模式 |
| 2 | STATE_24G_RECONN | 重连模式 |

### 数据类型常量

定义于 `format/g24_protocol_stack/24g_pair.format` (行 41-48)：

| 值 | 常量 | 说明 |
|----|------|------|
| 0x01 | DATATYPE_DEVICE1 | 鼠标设备 |
| 0x02 | DATATYPE_DEVICE2 | 键盘设备 |
| 0xAA | DATATYPE_BIND | 绑定请求（Step 2） |
| 0x55 | DATATYPE_CONFIG | 配置交换（Step 3） |
| 0x22 | DATATYPE_OK | 确认完成（Step 4） |
| 0xFF | DATATYPE_ATTEMP | 连接尝试（Step 1 / 重连） |

### 关键交换地址

| 地址变量 | 所属端 | 说明 | 交换方向 |
|----------|--------|------|----------|
| `mem_24g_device_addr` | 发射端 | 鼠标/键盘的地址 | Step 1 发给 Dongle |
| `mem_24g_receiver_addr` | 发射端 | 存储 Dongle 的地址 | Step 2 从 Dongle ACK 获取 |
| `mem_24g_lap` | 接收端 | Dongle 自身的 LAP 地址 | Step 2 通过 ACK 发给发射端 |
| `mem_24g_transmitter_addr` | 接收端 | 存储发射端的地址 | Step 1 从发射端获取 |
| `mem_24g_device1_addr` | 接收端 | 鼠标地址（存储） | Step 1 保存 |
| `mem_24g_device2_addr` | 接收端 | 键盘地址（存储） | Step 1 保存 |

---

## 对码参数配置

定义于 `format/g24_protocol_stack/24g_pair.format` (行 3-52)：

| 参数 | 大小 | 说明 |
|------|------|------|
| `mem_24g_pair_addr` | 4 bytes | 对码使用的 RF 地址 |
| `mem_24g_pair_ch` | 1 byte | 对码使用的 RF 通道 (默认 0x4E) |
| `mem_24g_pair_tx_power` | 1 byte | 对码发射功率 |
| `mem_24g_pair_sm` | 1 byte | 对码状态机当前状态 |
| `mem_24g_pair_no_ack` | 1 byte | 无 ACK 标志 |
| `mem_24g_pair_timeout_init` | 2 bytes | 对码超时初始值 |
| `mem_24g_pair_timeout` | 2 bytes | 对码超时计数器 |
| `mem_24g_pair_count` | 2 bytes | 对码尝试次数 |
| `mem_24g_device1_bind_step` | 1 byte | 鼠标绑定步骤 (0→1→2→成功) |
| `mem_24g_device2_bind_step` | 1 byte | 键盘绑定步骤 (0→1→2→成功) |
| `mem_24g_bind_device_status` | 1 byte | 绑定状态 (bit0=鼠标, bit1=键盘) |
| `mem_24g_bind_payload` | 8 bytes | ACK payload 缓冲区 |
| `mem_24g_pair_switch` | 1 byte | 快速配对模式开关 |
| `mem_24g_bind_mode_continue` | 1 byte | 对码模式持续时间控制 (约 400ms/单位) |
