# 对码流程解析

每个对码发射端数据包 7 字节，结构固定：

```
┌──────────┬──────────────┬─────┬──────┬─────────┬─────────────────┬─────┐
│ Preamble │ Access Addr  │ HEC │ Type │ Control │    Payload      │ CRC │
│  5 字节   │    4 字节    │ 1B  │  1B  │   1B    │     7 字节      │ 3B  │
└──────────┴──────────────┴─────┴──────┴─────────┴─────────────────┴─────┘
                 ↑          └────────── PDU (10 字节) ──────────────┘
          对码时 = 0x101520       └────────── txlen = 10 ──────────┘
          (mem_24g_pair_addr)
							          Control = 7<<3 | PID<<1 | ACK = 0x39
```
对码接收端数据包 8 字节，结构固定：

```
┌──────────┬──────────────┬─────┬──────┬─────────┬─────────────────┬─────┐
│ Preamble │ Access Addr  │ HEC │ Type │ Control │  ACK Payload    │ CRC │
│  5 字节   │    4 字节    │ 1B  │  1B  │   1B    │     8 字节      │ 3B  │
└──────────┴──────────────┴─────┴──────┴─────────┴─────────────────┴─────┘
                                             └─── txlen = 11 ───┘
							          Control = 8<<3 | PID<<1 | NO_ACK = 0x41
```


---

#### Step 1：连接尝试（ATTEMP）

**发射端发送**

发射端发送载荷（7 字节）：

```
[0] = 0xFF              ← DATATYPE_ATTEMP
[1] = 0x01 / 0x02       ← 设备类型 (DEVICE1=鼠标 / DEVICE2=键盘)
[2-5]                   ← 发射端自身地址 (mem_24g_device_addr, 4字节)
[6] = 0x00              ← 填充
```

**接收端（Dongle）处理** 

Dongle 收到后只**保存地址**。注意此时 Dongle 不与发射端做地址校验——任何发射端发来的 ATTEMP，只要类型匹配且该设备尚未绑定（`mem_24g_deviceX_bind_disable == 0`），Dongle 都会接受并保存地址。

ACK 回复由底层 `g24_transmit_ack` 处理，不携带特殊 payload（`mem_24g_bind_payload` 此时尚未设置，ACK 中无有效数据）。

---

#### Step 2：绑定请求（BIND）

**发射端发送**

发射端发送载荷：

```
[0] = 0xAA              ← DATATYPE_BIND
[1] = 0x01 / 0x02       ← 设备类型
[2-5]                   ← 设备地址
[6] = 0x00
```

**发射端收到 ACK** 

`pair_switch=1` 时，发射端从 ACK 中**知道了 Dongle 的地址**，存到 `mem_24g_receiver_addr`。

**接收端（Dongle）处理** 

ACK 发送（8 字节）：

```
[0] = 0x01 / 0x02       ← DATATYPE_DEVICE1 或 DEVICE2
[1] = 0x80              ← 标记位
[2-7]                   ← Dongle 自己的 LAP 地址 (6字节)
```

---

#### Step 3：配置交换（CONFIG）

**发射端发送**

`pair_switch=1` 时发送载荷：

```
[0] = 0x55              ← DATATYPE_CONFIG
[1] = 0x01 / 0x02       ← 设备类型
[2-5]                   ← Dongle 的地址 (mem_24g_receiver_addr, 上一步从 ACK 获取)
[6] = 0x00
```

发射端把上一步接收到的 Dongle 地址**回传给 Dongle**。

`pair_switch=0` 时发送载荷：

```
[0] = 0x55
[1] = 0x01 / 0x02
[2-5]                   ← 发射端自己的地址 (mem_24g_device_addr)
[6] = 0x00
```

发送含义：**"我确认你的地址是 XXX"**（pair_switch=1）/ **"这是我的地址"**（pair_switch=0）

**发射端收到 ACK** 

`pair_switch=1` 时，发射端验证 Dongle ACK 中回传的地址是否等于自己的 `mem_24g_device_addr`——**双向确认完成**。

**接收端（Dongle）处理** 

`pair_switch=1` 时，Dongle 会**校验发射端发回来的地址是否等于自己的 LAP**——安全确认，防止配错设备。

Dongle ACK payload（`pair_switch=1` 时）：

```
[0] = 0x01 / 0x02       ← DATATYPE_DEVICE1 或 DEVICE2
[1] = 0x80
[2-5]                   ← mem_24g_transmitter_addr（发射端地址）
```

Dongle ACK payload（`pair_switch=0` 时）：

```
[0] = 0x01 / 0x02
[1] = 0x80
[2-7]                   ← LAP 地址
```

Dongle 回复含义（pair_switch=1）：**"我确认你的地址是 XXX"**，将发射端地址回传做双向校验。

---

#### Step 4：确认完成（OK）

**发射端发送**

发射端发送载荷：

```
[0] = 0x22              ← DATATYPE_OK
[1] = 0x01 / 0x02       ← 设备类型
[2-5]                   ← 设备地址
[6] = 0x00
```

发送含义：**"绑定完成"**

**发射端收到 ACK** 


**接收端（Dongle）处理** 

Dongle ACK payload（与 Step 3 相同）：

```
[0] = 0x01 / 0x02       ← DATATYPE_DEVICE1 或 DEVICE2
[1] = 0x80
[2-7]                   ← LAP 地址（pair_switch=0）或发射端地址（pair_switch=1）
```

Dongle 回复含义：**"绑定完成"**

---



## 发射端重连流程

#### 发射端重连数据包结构

空中 RF 帧
```
┌──────────┬──────────────┬─────┬──────┬──────┬──────────────────┬─────┐
│ Preamble │ Access Addr  │ HEC │ Type │ Ctrl │    Payload       │ CRC │
│  5 字节   │    4 字节    │ 1B  │  1B  │ 0x30 │     6 字节       │ 3B  │
└──────────┴──────────────┴─────┴──────┴──────┴──────────────────┴─────┘
```


重连不是每个周期都发送 ATTEMP 包，而是采用**突发 + 退避**的节奏：

```
reconn_count & 0x01F8 == 0  → 执行调度（真正发送 ATTEMP）
否则                         → 仅递增计数，跳过
```

其中 `0x01F8` 的二进制为 `0000 0001 1111 1000`，掩码提取 bit3-bit8。`count % 512 < 8` 时触发，即每 512 个周期发动一次 8 连发，中间 504 个周期静默。假设调度周期约 1ms，则每 512ms 密集尝试 8 次。

### 发送与 ACK 验证

**1. 发送 ATTEMP 包：**

调用 `g24_reconn_data_prep` 组装数据包 → `g24_transmit_receive_ack` 发射并等待 ACK。ATTEMP 包结构（6 字节）：

```
[0] = 0xFF              ← DATATYPE_ATTEMP
[1] = 设备类型            ← TYPE_MS 或 TYPE_KB（低 3 位）
[2-5]                   ← 设备地址 (mem_24g_device_addr)
```

**2. 同步/ACK 失败：**

`sync` 失败（未检测到同步字）或 `user3` 失败（ACK 超时/CRC 错）→ 跳 `g24_reconn_device_fail`，根据 `mem_24g_reconn_type` 切换地址后重试。

**3. ACK 成功，解析并验证：**

调用 `g24_ackpayload_parse` 解析 ACK payload，然后执行两级地址校验：

```
第一级：ACK 地址 == mem_24g_pair_addr（对码广播地址）？
  → 是：Dongle 还在 BIND 模式，双方无绑定关系
        直接跳 g24_pair_start 走完整四步握手对码

第二级：ACK 地址 == mem_24g_device_addr（自身设备地址）？
  → 是：Dongle 已识别本设备，用的是专属地址回复
        调用 g24_store_receiver_addr 保存 Dongle 地址
        进入 g24_reconn_dispatch_end 宣告重连成功
```

**4. 重连成功收尾 — `g24_reconn_dispatch_end`：**

- 清除 `STATE_24G_RECONN`，退出重连模式
- 清零 `mem_24g_pair_timeout`、`mem_24g_reconn_count`、`mem_24g_reconn_timeout`
- 清空发送缓冲区 (`g24_txbuf_clear`)
- 发送 `BT_EVT_24G_ATTEMPT_SUCCESS` 事件通知 UI 层

### 失败轮换

失败时递增 `mem_24g_reconn_count`，取 bit8 位判断奇偶，根据 type 在两种地址之间切换：

| type                   | bit8=0（偶数轮）     | bit8=1（奇数轮）     |
| ---------------------- | --------------- | --------------- |
| FAST_CONN_AND_RECEIVER | receiver_addr   | fast_conn_addr  |
| FAST_CONN_AND_3_0_ADDR | 3.0 device_addr | fast_conn_addr  |
| RECEIVER_AND_3_0_ADDR  | 3.0 device_addr | receiver_addr   |
| PAIR_AND_3_0_ADDR      | pair_addr（走对码）  | 3.0 device_addr |

---

## 接收端 ATTEMP 处理

接收端在三个场景下会收到 `DATATYPE_ATTEMP` 包：

### 1. 对码阶段 — 搜索模式 — `g24_search_mode_auto`

Dongle 对码成功后进入 `DONGLE_SEARCH` 模式（通过 `g24_search_mode_enable`），在已绑定的设备地址之间轮询监听。收到数据后按类型分发：

```
DATATYPE_DEVICE1 → 鼠标数据回调
DATATYPE_DEVICE2 → 键盘数据回调
DATATYPE_ATTEMP  → g24_data_receive_attemp → g24_data_attemp
```

ATTEMP 设备地址保存 — `g24_data_attemp`
```
从 rxdata_temp[3] 判断设备类型:
  DEVICE1 → g24_data_attemp_device1:
    提取 rxdata_temp[4-7] → mem_24g_transmitter_addr（保存发射端地址）
    提取 rxdata_temp[4-7] → mem_24g_device1_addr（保存为鼠标地址）
  DEVICE2 → g24_data_attemp_device2:
    提取 rxdata_temp[4-7] → mem_24g_transmitter_addr
    提取 rxdata_temp[4-7] → mem_24g_device2_addr（保存为键盘地址）
```

前提是该设备尚未绑定 (`mem_24g_deviceX_bind_disable == 0`)。在dongle上电配对过后，这个标志位会被置位。

### 2. 工作模式接收 — `g24_receive_packet_parse`

Dongle 在工作模式的接收循环中处理重连包：

```asm
    fetch 1,mem_24g_rxdata_temp+2
    beq DATATYPE_DEVICE1,g24_data_device1   ; 0x01 = 普通鼠标数据
    beq DATATYPE_DEVICE2,g24_data_device2   ; 0x02 = 普通键盘数据
    beq DATATYPE_ATTEMP,g24_data_receive_attemp   ; 0xFF = 重连包
```

收到 `0xFF` 后走 `g24_data_receive_attemp` → `g24_data_attemp`（与对码 Step 1 相同逻辑）

重连包**不触发 ACK 回复**，Dongle 收到后只更新地址记录，然后切换 RF 地址来接收后续的正常数据。

### 3. 带中止标记的 ATTEMP — `g24_data_attemp_deviceX_abort`

若收到带 `bit_abort` 标志的 DEVICE1 包，Dongle 会强制清除 `mem_24g_device1_bind_disable`，解除绑定并重新保存地址。这用于发射端主动触发解绑重连的场景。

---

## Dongle 地址状态与重连行为的关系

Dongle 在不同工作模式下使用的 RF 地址不同，这直接决定了重连的结果：

|Dongle 状态|监听地址|发射端发往 receiver_addr|结果|
|---|---|---|---|
|BIND 模式（未绑定/刚上电）|`pair_addr`（对码广播）|receiver_addr（不对）|ACK 地址 == pair_addr → 触发完整对码|
|SEARCH 模式（已绑定）|设备专属地址（device1/device2 轮询）|receiver_addr（匹配）|ACK 地址 == device_addr → 重连成功|
|WORK 模式（正常通信）|当前活跃设备地址|receiver_addr（匹配）|正常数据通信|

关键结论：只要 Dongle 不掉电（不掉绑定状态），发射端唤醒后直接用 `receiver_addr` 即可重连成功。`pair_addr` 的校验分支主要兜底 Dongle 丢失绑定信息的边界场景。

---

# 2.4G通讯协议帧解析
## 2.G通讯RF 帧

```
┌──────────┬──────────────┬─────┬──────┬─────────┬──────────────────┬─────┐
│ Preamble │ Access Addr  │ HEC │ Type │ Control │     Payload      │ CRC │
│  5 字节   │    4 字节    │ 1B  │  1B  │   1B    │  8B(鼠标)/10B(键盘)│ 3B  │
└──────────┴──────────────┴─────┴──────┴─────────┴──────────────────┴─────┘
```

Access Address 为设备专属地址（device_addr / LAP），非对码广播地址 0x101520。

## PDU（g24_transmit_prep，24g.prog 行 282-322）

```
PDU偏移     │ 大小 │ 内容
────────────┼──────┼──────
txpayload[0]│ 1B   │ HEC（mem_24g_syncword_crc8）
txpayload[1]│ 1B   │ Type（TYPE_MS=0x01 / TYPE_KB=0x02，取低 3 位 bits_data）
txpayload[2]│ 1B   │ Control = datalen<<3 | PID<<1 | NO_ACK
txpayload[3]│    ...
           │      │ 应用层 payload（txbuf）
```

## 鼠标包（datalen=8, txlen=11, Control=0x40）

```
txbuf 偏移  │ 来源变量               │ 内容
───────────┼───────────────────────┼──────
txbuf[0]   │                       │ 0x00（累积标志，非累积时为 0）
txbuf[1]   │ mem_mouse_key         │ 按键（bit0=左键 bit1=右键 bit2=中键 bit3=侧键1 bit4=侧键2）
txbuf[2]   │ mem_mouse_x 低字节     │ X 位移低字节
txbuf[3]   │ mem_mouse_x 高字节     │ X 位移高字节
txbuf[4]   │ mem_mouse_y 低字节     │ Y 位移低字节
txbuf[5]   │ mem_mouse_y 高字节     │ Y 位移高字节
txbuf[6]   │ mem_mouse_z           │ 滚轮 Z（有符号）
txbuf[7]   │ mem_mouse_tz          │ 倾斜滚轮 TZ（有符号）
```

数据来源：`mouse_24g_package_data`（app_mouse.prog 行 4549-4584）

有累积时：新增数据与上次 `txbuf[1][6][7]` 按位或 / 相加合并（行 4558-4577）。

## 键盘包（datalen=10, txlen=13, Control=0x50）

```
txbuf 偏移  │ 来源变量                         │ 内容
───────────┼───────────────────────────——————─┼──────
txbuf[0]   │                                  │ 0x00
txbuf[1-9] │ mem_customer_key_press / release │ 键值（9 字节，无按键时为 release）
```

---

# 现有的对于串码问题的解决方案：

### 发射端：p_g24_transmit_prep_rssi

#### 三重门控——决定是否执行

```
STATE_24G_PAIR  → 跳过     ; 配对的包结构不同，走 g24_pair_dispatch
STATE_24G_RECONN → 跳过     ; 回连的包结构不同，走 g24_reconn_dispatch
txpayload[1] bit7=1 → 跳过  ; 已经插入过MAC了，防重复
```

只有三个条件全部不满足时，才往下执行——即**正常通讯包、且首次经过此路径**。
#### 改包——插入身份信息

ROM 原版 `g24_transmit_prep_rssi` 只插入 1 字节 RSSI。替换为插入 4 字节 MAC 地址 + 1 字节校验和，分为三步：

**第一步**，在 `data_type`（`txpayload[1]`）上打标记：

```asm
txpayload[1] = data_type | 0x80   ; bit7置1 → 声明"新格式包"，同时锁住防重入
```

**第二步**，把 control 字节（`txpayload[2]`）中的数据长度 +5：

```asm
datalen = (control >> 3 & 0x1F) + 5
control = (datalen << 3) | (control & 0x07)   ; pid和no_ack保持不变
```

**第三步**，在包尾追加数据。先调 `cal_sum` 对 4 字节 MAC 地址做累加求和得到 1 字节校验和，然后写 MAC 和校验和到包体尾部（偏移 = datalen + 3）。

```
TX包最终结构:
[0] HEC
[1] data_type (bit7=1, 新格式标记)
[2] control   (datalen +5)
[3..N-6] 用户数据
[N-5..N-2] MAC地址 (4字节)
[N-1]     校验和   (1字节)
```

---

# 接收端：p_g24_receive_addr

1.消息包类型过滤检测：
只有 `DATATYPE_ATTEMP` 类型的包才进入地址校验。bind 流程中的包（`DATATYPE_BIND`、`DATATYPE_CONFIG`、`DATATYPE_OK`）不需要校验地址

2.设备识别：
根据`mem_24g_rxbuf[0]`中Device ID来检测设备类型决定走哪条校验分支

3.**三段式校验**
1. **包格式门禁** — 检查 `mem_g24_package_format_flag` 中对应设备的`data type`的bit7 是否已置位。如果从未收到过该设备带格式标志的包，说明不是带有防串码机制的数据包，直接略过（防止误伤）。
2. **提取发送端地址** — 调用 `p_g24_receiver_contr_search` 根据 payload 长度算出地址字段在接收缓冲区中的位置，拷贝 5 字节（4 字节地址 + 1 字节 checksum）到临时变量。
3. **Checksum 比对** — 如果本地还未存储该设备的绑定地址（`mem_24g_device1_addr` 或 `mem_24g_device2_addr` 为空），说明尚未绑定，直接放过。如果已有绑定地址，则对本地地址用逐字节累加取低 8 位的方式生成 checksum，与接收包中附带的 checksum 比对。**匹配则放行，不匹配则 `enable_user2`**，回到调用方 `p_g24_receive_data_prase` 中，通过 `branch g24_rx_interrupt_clear,user2` 丢弃整包数据。

**配套机制：地址的来源**

校验之前还有两个配套动作。mouse 和 keyboard 各自的回调函数 `p_dongle_g24_mouse` 和 `p_dongle_g24_kb` 会在首次收到某个设备的数据时，将当前接收地址写入 `mem_24g_device1_addr` 或 `mem_24g_device2_addr`，后续每次收到数据还会比较地址是否一致、不一致直接跳过。`p_dongle_usb_dispatch` 中每轮更新 `mem_dg_connected_flag` 连接状态位图，告诉上层哪些设备当前活跃。

## 简单来说：
对收到的每一包 `DATATYPE_ATTEMP` 类型数据，根据 device ID 位图判断是 mouse 还是 keyboard 发的，然后比对接收包中附带的地址 checksum 与本地存储的绑定设备地址 checksum 是否一致。不匹配的直接通过 user2 中断丢弃，确保只处理真正绑定过的那台设备的数据。